name: NPM Publish

on:
  release:
    types:
      - published

jobs:
  npm-publish:
    permissions:
      contents: read
      id-token: write
    uses: hemilabs/actions/.github/workflows/npm-publish.yml@e67f2b7d682accaeb026ebe393f8a161b2daa35b # v2.2.0
    secrets: inherit
