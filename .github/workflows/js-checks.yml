name: JS Checks

on:
  pull_request:
  push:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  js-checks:
    uses: hemilabs/actions/.github/workflows/js-checks.yml@8f87619b7f0122c39e14a32514ab3e4e0d4c3966 # v2.3.0
